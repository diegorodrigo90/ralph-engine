# Configuration

The Rust-first reboot is starting by hardening repository and runtime contracts first.

Project configuration contracts such as `.ralph-engine/config.yaml` and plugin defaults remain part of the target architecture and will be reintroduced on top of the new core incrementally, with tests as the contract.
