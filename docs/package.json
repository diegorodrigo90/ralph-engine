{
	"private": true,
	"scripts": {
		"prepare-content": "node ../scripts/prepare-docs-content.mjs",
		"dev": "npm run prepare-content && vitepress dev .content",
		"build": "npm run prepare-content && vitepress build .content --outDir .vitepress/dist",
		"preview": "npm run prepare-content && vitepress preview .content"
	},
	"devDependencies": {
		"vitepress": "^1.6.4"
	}
}
