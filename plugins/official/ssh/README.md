# official/ssh

SSH provider plugin for typed remote-control and transport surfaces.

## Surfaces

- Kind: `remote_control`
- Runtime hooks:
  - `remote_control_registration`
- Provider contributions:
  - `official.ssh.remote`

## What it owns

- the typed remote-control provider descriptor
- localized plugin metadata
