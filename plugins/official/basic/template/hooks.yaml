# ralph-engine hooks
#
# NOTE: Hook execution is not yet implemented in the runtime.
# This file defines the intended hook points for future use.
# Configure your quality gates here — they will be executed
# when the hooks system is wired up.

session_start:
  steps: []

work_item_start:
  steps: []

after_agent:
  steps:
    - name: "Tests"
      run: ""
      required: true
    - name: "Build"
      run: ""
      required: false

work_item_finish:
  steps: []

session_end:
  steps: []
