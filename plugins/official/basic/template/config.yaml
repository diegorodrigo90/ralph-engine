schema_version: 1
default_locale: en

plugins:
  - id: official.basic
    activation: enabled

mcp:
  enabled: false
  discovery: official_only
  servers: []

budgets:
  prompt_tokens: 8192
  context_tokens: 32768

# Uncomment and configure to use ralph-engine run:
# run:
#   workflow_plugin: official.bmad
#   agent_plugin: official.claude
#   agent_id: official.claude.session
