# packaging/npm

The npm packaging channel remains part of the official Ralph Engine distribution contract.

During the Rust-first reboot this package stays private until the release pipeline is reconnected to Rust artifacts and provenance.
